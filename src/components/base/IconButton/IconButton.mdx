import { ArgTypes, Canvas, Meta, Story } from "@storybook/blocks";
import * as Stories from "./IconButton.stories";

<Meta of={Stories} />

# IconButton

IconButton base component.

## Props

<ArgTypes />

## Predefined properties

### IconPlacement

There are two icon placement predefined properties: left and right.

<Canvas of={Stories.IconPlacement} withToolbar />

### Format

There are three format predefined properties: default, semiRounded and rounded.

<Canvas of={Stories.PredefinedFormat} withToolbar />

### Size

There are three size predefined properties: small, medium and large.

<Canvas of={Stories.PredefinedSize} withToolbar />

### Icon Format

There are three icon format predefined properties: default, semiRounded and rounded.

<Canvas of={Stories.PredefinedIconFormat} withToolbar />

## Custom properties

### Color

IconButton color can be modified.

<Canvas of={Stories.Color} withToolbar />

### Format

IconButton format can be modified.

<Canvas of={Stories.CustomFormat} withToolbar />

### Size

IconButton size can be modified.

<Canvas of={Stories.CustomSize} withToolbar />

### Text property

IconButton text property can be modified.

<Canvas of={Stories.TextProperty} withToolbar />

### Icon Height

IconButton icon height can be modified.

<Canvas of={Stories.IconHeight} withToolbar />

### Icon Width

IconButton icon width can be modified.

<Canvas of={Stories.IconWidth} withToolbar />

### Icon Height and Width

IconButton icon height and width can be modified.

<Canvas of={Stories.IconHeightAndWidth} withToolbar />

### Icon Format

IconButton icon format can be modified.

<Canvas of={Stories.CustomIconFormat} withToolbar />
